{
  "name": "pajros",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "watch:sass": "node-sass -o css sass --watch",
    "watch:browsersync": "browser-sync start --server --files * css/style.css",
    "watch:prefix": "autoprefixer-cli -o css/style.css css/style.css"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "autoprefixer": "^8.6.2",
    "browser-sync": "^2.24.4",
    "node-sass": "^4.9.0",
    "onchange": "^4.0.0"
  },
  "devDependencies": {
    "autoprefixer-cli": "^1.0.0"
  }
}
